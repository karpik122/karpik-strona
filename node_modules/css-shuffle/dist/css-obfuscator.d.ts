import { Renamer } from "./renamer.js";
export declare class CSSObfuscator {
    private renamer;
    constructor(renamer: Renamer);
    private obfuscateName;
    /**
     * Parse CSS source and obfuscate all class selectors, ID selectors,
     * and custom property names (--*) throughout rules, @property at-rules,
     * and var() references.
     */
    obfuscate(css: string): Promise<string>;
}
//# sourceMappingURL=css-obfuscator.d.ts.map